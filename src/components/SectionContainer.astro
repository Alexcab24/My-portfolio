---
const { class: className, id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className} w-full mx-auto lg:w-[1020px] pb-24 animate-fade-in animate-duration-slower`}
>
  <slot />
</section>
